#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

echo "[pre-push] Running required CI gates"
CI=1 NX_TASKS_RUNNER_DYNAMIC_OUTPUT=false bun run ci:required
