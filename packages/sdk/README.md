# @mdcms/sdk

Client SDK package boundary for MDCMS API consumption.

## Current Status

This package is intentionally scaffolded in CMS-1 to establish the `@mdcms/sdk` contract boundary and build/typecheck targets. API client functionality is implemented in downstream SDK tasks.

## Build

- `bun nx build sdk`
- `bun nx typecheck sdk`
